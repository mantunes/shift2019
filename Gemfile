# frozen_string_literal: true

source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'serialport'
gem 'fftw3'
gem 'coreaudio', '~> 0.0.12'
gem 'thread'